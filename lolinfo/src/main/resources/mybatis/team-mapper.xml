<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="team">
	
	<!-- 등록 -->
	<select id="sequence" resultType="int">
		select team_seq.nextval from dual
	</select>
	<insert id="insert">
		insert into team(
			team_id, tournament_id, team_name,
			team_top, team_jug, team_mid, team_ad, team_sup,
			team_ranking, team_note
		)
		values(
			#{teamId}, #{tournamentId}, #{teamName}, 
			#{teamTop}, #{teamJug}, #{teamMid}, #{teamAd}, #{teamSup},     
			#{teamRanking}, #{teamNote}  
		)
	</insert>

	
	<!-- 상세조회 -->
	<select id="selectListByTournamentId" resultType="tournamentDto">
		select * from team
		where tournament_id = #{tournamentId} order by tournament_year 
	</select>


</mapper>