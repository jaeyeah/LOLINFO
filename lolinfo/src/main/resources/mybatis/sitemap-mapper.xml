<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sitemap">

	<select id="selectStreamerList"
            resultType="com.lol.lolinfo.vo.SitemapVO">
        SELECT
            streamer_no AS target_no
        FROM streamer
        ORDER BY streamer_no

    </select>


    <select id="selectTournamentList"
            resultType="com.lol.lolinfo.vo.SitemapVO">
        SELECT
            tournament_id AS target_no
        FROM tournament
        ORDER BY tournament_id

    </select>

	<!-- 대회 참가 이력이 있는 스트리머 -->
	<select id="selectTournamentStreamerList" resultType="sitemapVO">
	    SELECT DISTINCT
	        tm.streamer_no AS target_no
	    FROM team_member tm
	    WHERE tm.streamer_no IS NOT NULL
	</select>
	
	<!-- CK 기록이 있는 스트리머 -->
	<select id="selectCkStreamerList" resultType="sitemapVO">
	    SELECT DISTINCT
	        cp.ck_streamer AS target_no
	    FROM ck_participant cp
	    WHERE cp.ck_streamer IS NOT NULL
	</select>
	
</mapper>